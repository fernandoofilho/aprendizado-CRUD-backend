# Copie este arquivo para `.env` (o `.env` não vai para o git).
# O Prisma lê DATABASE_URL para saber onde está o Postgres.

DATABASE_URL="postgresql://crud_user:passwordEasy@localhost:5432/aprendizado"
PORT=3001
FRONTEND_URL="http://localhost:5173"
